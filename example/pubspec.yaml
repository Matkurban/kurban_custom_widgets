name: example
description: "A Flutter UI package that provides internationalized widgets and components for building multilingual applications."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.7.2

dependencies:
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  kurban_custom_widgets:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
